#include "Image.h"
#include "StickerSheet.h"

int main() {
  
  // Image noir;        noir.readFromFile("../noir_schizo.png");
  // Image i;           i.readFromFile("../thomasCSAW.png");
  // Image faygo;       faygo.readFromFile("../faygo.png");
  // Image sigpwny;     sigpwny.readFromFile("../sigpwny.png");
  // Image amongusblue; amongusblue.readFromFile("../amongusblue.png");
  // Image amongus;     amongus.readFromFile("../amongus.png");
  // Image plate;       plate.readFromFile("../plate.png");
  // Image spoingus;    spoingus.readFromFile("../spoingus.png");
  // Image anime;       anime.readFromFile("../anime.png");
  // Image pl;          pl.readFromFile("../PL.png");
  // Image hackmeme;    hackmeme.readFromFile("../hackmeme.png");
  // Image hello;       hello.readFromFile("../hello.png");
  // Image logo;        logo.readFromFile("../logo.png");
  // Image shark;       shark.readFromFile("../shark.png");
  // Image green;       green.readFromFile("../green.png");
  
  // green.scale(0.2);
  // amongus.scale(0.45);
  // pl.scale(0.7);
  // i.scale(0.2);
  // hello.scale(2);
  // shark.scale(0.5);
  // //sigpwny.scale(0.9);
  // hackmeme.scale(0.4);
  // plate.scale(0.3);
  // amongusblue.scale(0.7);
  // //hello.scale(0.8);
  // logo.scale(0.6);
  // anime.scale(0.7);

  // StickerSheet sheet(noir, 18);
  
  // sheet.addSticker(i, 20, 600);
  // sheet.addSticker(hello, 900, 1700);
  // sheet.addSticker(spoingus, 1000, 1);
  // sheet.addSticker(hackmeme, 400, 250);
  // sheet.addSticker(sigpwny, 600, 600);
  // sheet.addSticker(amongus, 20, 1500);
  // sheet.addSticker(pl, 40, 300);
  // sheet.addSticker(faygo, 1200, 1200);
  // sheet.addSticker(amongusblue, 500, 1540);
  // sheet.addSticker(plate, 750, 300);
  // sheet.addSticker(logo, 600, 600);
  // sheet.addSticker(anime, 100, 1150);
  // sheet.addSticker(shark, 900, 450);
  // sheet.addSticker(green, 250, 1400);
  
  // Image myImage = sheet.render();

  // myImage.writeToFile("../myImage.png");

  return 0;
}
